## GeoInsight BD — RabbitMQ broker configuration
## Topology is applied by rabbitmq-init (management API) after DEFAULT_USER exists.
## Do NOT load_definitions here with an empty users[] — that wipes RABBITMQ_DEFAULT_USER.

# Prometheus metrics (enabled via enabled_plugins)
prometheus.return_per_object_metrics = true

# Classic queues for gov / AI workloads
default_vhost = /
default_user_tags.administrator = true
