; Local / VPS — both pools hit primary (replica optional via --profile with-replica)
[databases]
geoinsight_write = host=postgres port=5432 dbname=geoinsight_db
geoinsight_read  = host=postgres port=5432 dbname=geoinsight_db

[pgbouncer]
listen_addr = 0.0.0.0
listen_port = 6432
auth_type = plain
auth_file = /tmp/userlist.txt
pool_mode = transaction
; Sized for 4–8GB VPS (was 1000/40 — too aggressive)
max_client_conn = 200
default_pool_size = 15
min_pool_size = 2
reserve_pool_size = 5
max_db_connections = 50
server_reset_query = DISCARD ALL
ignore_startup_parameters = extra_float_digits,search_path,application_name
admin_users = geoinsight_admin
